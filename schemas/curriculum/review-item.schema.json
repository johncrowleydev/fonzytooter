{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$comment": "Code generated by server/cmd/curriculum-schema; DO NOT EDIT.",
  "$ref": "#/$defs/ReviewItemAuthoring",
  "$defs": {
    "ReviewItemAuthoring": {
      "additionalProperties": false,
      "properties": {
        "answer": {
          "description": "Authored answer shown after recall.",
          "type": "string"
        },
        "hint": {
          "description": "Optional hint available before revealing the answer.",
          "type": "string"
        },
        "id": {
          "description": "Stable review-item ID within the owning module.",
          "type": "string"
        },
        "objectiveIds": {
          "description": "Objective IDs practiced by this review item.",
          "items": {
            "type": "string"
          },
          "minItems": 1,
          "type": "array",
          "uniqueItems": true
        },
        "order": {
          "description": "Non-negative review-item order within the module.",
          "format": "int64",
          "minimum": 0,
          "type": "integer"
        },
        "prompt": {
          "description": "Question or retrieval prompt shown to the learner.",
          "type": "string"
        },
        "sourceLessonId": {
          "description": "ID of the lesson from which this item is derived.",
          "type": "string"
        }
      },
      "required": [
        "id",
        "order",
        "objectiveIds",
        "sourceLessonId",
        "prompt",
        "answer"
      ],
      "title": "Fonzytooter review item",
      "type": "object"
    }
  }
}
