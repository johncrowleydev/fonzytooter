{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$comment": "Code generated by server/cmd/curriculum-schema; DO NOT EDIT.",
  "$ref": "#/$defs/ExerciseAuthoring",
  "$defs": {
    "ExerciseAuthoring": {
      "additionalProperties": false,
      "properties": {
        "id": {
          "description": "Stable exercise ID within the owning module.",
          "type": "string"
        },
        "lessonId": {
          "description": "ID of the lesson that owns this exercise.",
          "type": "string"
        },
        "objectiveIds": {
          "description": "Objective IDs assessed by this exercise.",
          "items": {
            "type": "string"
          },
          "minItems": 1,
          "type": "array",
          "uniqueItems": true
        },
        "order": {
          "description": "Non-negative exercise order within its lesson.",
          "format": "int64",
          "minimum": 0,
          "type": "integer"
        },
        "prompt": {
          "description": "Learner-facing exercise prompt.",
          "type": "string"
        },
        "starterCode": {
          "description": "Initial Python code loaded into the editor.",
          "type": "string"
        },
        "tests": {
          "description": "Deterministic tests used to check the exercise.",
          "items": {
            "$ref": "#/$defs/ExerciseTestAuthoring"
          },
          "minItems": 1,
          "type": "array"
        },
        "title": {
          "description": "Exercise title shown to the learner.",
          "type": "string"
        }
      },
      "required": [
        "id",
        "title",
        "lessonId",
        "order",
        "objectiveIds",
        "prompt",
        "starterCode",
        "tests"
      ],
      "title": "Fonzytooter exercise",
      "type": "object"
    },
    "ExerciseTestAuthoring": {
      "additionalProperties": false,
      "properties": {
        "code": {
          "description": "Python test code executed by the browser exercise runner.",
          "type": "string"
        },
        "id": {
          "description": "Stable test ID within this exercise.",
          "type": "string"
        },
        "title": {
          "description": "Test title shown in results when appropriate.",
          "type": "string"
        },
        "visibility": {
          "description": "Whether test details are visible or hidden from the learner.",
          "enum": [
            "visible",
            "hidden"
          ],
          "type": "string"
        }
      },
      "required": [
        "id",
        "title",
        "visibility",
        "code"
      ],
      "type": "object"
    }
  }
}
