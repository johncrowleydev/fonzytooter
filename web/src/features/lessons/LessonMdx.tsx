// TEMPORARY: remove this compile stub when the parallel LessonMdx implementation lands.
export function LessonMdx({ source }: { source: string }) {
  return <div className="whitespace-pre-wrap">{source}</div>
}
